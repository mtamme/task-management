spring:
  datasource:
    url: jdbc:h2:mem:task-management;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    clean-disabled: false

outbox:
  enabled: false
